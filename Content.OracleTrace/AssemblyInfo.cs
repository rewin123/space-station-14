// Сценарии оракула НЕ параллелятся. Причина не в скорости: каждый сценарий
// пишет золотую трассу, и трасса обязана быть воспроизводимой. Два сценария,
// крутящиеся одновременно на разных парах, делят один пул, один пул делит
// генератор сидов, и порядок выдачи сидов зависит от планировщика NUnit.
// Один поток — один детерминированный порядок.

[assembly: LevelOfParallelism(1)]
[assembly: NonParallelizable]
